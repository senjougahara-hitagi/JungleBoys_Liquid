    <nav class="navbar navbar-expand-md navbar-light bg-faded">
      <a class="navbar-brand" href="/">Jungle Trips</a>
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <%= form_tag(rooms_path, method: :get, :class => 'form-inline mr-auto' ) do %>
          <%= text_field_tag :room_name, params[:room_name], :class =>'form-control mr-sm-2' %>
          <%= submit_tag 'Search', name:nil, :class => 'btn btn-light my-2 my-sm-0' %>
        <% end %>

        <ul class="navbar-nav">
          <!-- đã đăng nhập -->
          <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="javascript:void(0)" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= image_tag(current_user.user_pic, :height=>'25px', :width=>'25px', :style => 'margin-right: 5px;border-radius: 25%') unless current_user.user_pic.blank? %>
              <p style="display:inline"><%= current_user.username %></p> 
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
              <a class="dropdown-item" href="/users/<%= current_user.id %>">Profile</a>
              <a class="dropdown-item" href="javascript:void(0)">Settings</a>
              <div class="dropdown-divider"></div>
              <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
          <!-- kết thúc phần đã đăng nhập -->
          <% else %>
          <!-- chưa đăng nhập -->
          <li class="nav-item">
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
          <!-- kết thúc phần chưa đăng nhập -->
          <% end %>
        </ul>
      </div>
    </nav>
